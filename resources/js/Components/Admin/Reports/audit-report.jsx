import ReportView, { createStats } from "./report-view";
export default function AuditReport(props) { return <ReportView {...props} title="System Activity / Audit Report" description="Administrative actions, user activity, and system logs." stats={createStats(["Admin Activities", "User Activities", "System Events", "Total Logs"])} filters={[{ key: "type", label: "Activity Type", options: ["Admin", "User", "System"] }]} columns={[{ key: "date", label: "Date" }, { key: "actor", label: "Actor" }, { key: "type", label: "Type" }, { key: "action", label: "Action" }, { key: "ip", label: "IP Address" }]} />; }
