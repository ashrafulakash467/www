<?php

namespace Tests;

use Illuminate\Foundation\Testing\TestCase as BaseTestCase;

/**
 * Base class for application tests.
 *
 * Unlike a plain unit test, Laravel's base class boots the application container,
 * routes, configuration, database connections, middleware, and helper methods.
 */
abstract class TestCase extends BaseTestCase
{
    // Shared test helpers can be added here when multiple test classes need them.
    //
}
